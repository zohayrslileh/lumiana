# esbuild + Lumiana

This example bundles esbuild's JavaScript API into the browser while Lumiana retains its native
platform executable in the engine deployment. Click **Compile TypeScript** to run
`esbuild.transform()` and display the generated JavaScript.

```sh
bun install
bun dev
```
